# Shell scripts run on Linux (CI, containers), where a carriage return is a syntax error:
# keep them LF whatever the checkout. Everything else is left to Git's own line-ending handling.
*.sh text eol=lf
