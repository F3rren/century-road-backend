# Environment: .env is production (compose-prod.yml), .env.dev is development (compose-dev.yml).
# Only the .example files are committed.
.env
.env.dev

# Maven
service/*/target/

# Node
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Docker volumes (if accidentally created locally)
infra/grafana/data/
infra/prometheus/data/

# OS
.DS_Store
Thumbs.db
